# Forked from [Slither](https://github.com/crytic/slither)

### Init python bash command

```bash

# for ubuntu user 'sudo apt-get install python3-venv'
python3 -m venv .venv

source .venv/bin/activate

# deactive venv
deactivate
```
